import { ScrollOnDragDirective } from './scroll-on-drag.directive';

describe('ScrollOnDragDirective', () => {
  test('should create an instance', () => {
    const directive = new ScrollOnDragDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
