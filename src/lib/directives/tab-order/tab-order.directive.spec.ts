import { TabOrderDirective } from './tab-order.directive';

describe('PopupDirective', () => {
  it('should create an instance', () => {
    const directive = new TabOrderDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
